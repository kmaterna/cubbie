# Deal with unwrapping errors
# Either solve for phase jumps, or exclude the bad pixels. 
# July 1, 2018


def main_function(staging_directory, outdir):
	print("Please write code to solve or exclude unwrapping errors.");
	print("Eventually will deal with unwrapping errors for files in %s, and store the result in %s " % (staging_directory, outdir) )
	return;